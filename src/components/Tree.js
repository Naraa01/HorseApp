import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Tree = () => {
  return (
    <View>
      <Text>Tree</Text>
    </View>
  );
};

export default Tree;

const css = StyleSheet.create({});
